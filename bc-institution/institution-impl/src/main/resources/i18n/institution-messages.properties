# All message keys for the Institution module (Institutioner)
institution.welcome=Welcome to the Institution module!

# Institution ID validation
institution.id.required=Institution ID is required
institution.id.invalid=Invalid Institution ID format

# Institution Name validation
institution.name.required=Name is required
institution.name.length.invalid=Name is too long

# Institution Address validation
institution.address.required=Address is required
institution.address.length.invalid=Address is too long

# NotFoundException messages
# The {id} placeholder matches the Map.of("id", query.id()) in your service
institution.notFound=Institution record with ID {id} was not found

# General Institution request errors
institution.request.body.invalid=Institution data is invalid
institution.persistence.error=An error occurred while saving institution data