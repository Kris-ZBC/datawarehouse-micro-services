<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
   <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>local.sop.datawarehouse</groupId>
        <artifactId>parent</artifactId>
        <version>${revision}</version>
        <relativePath>../parent-POM/pom.xml</relativePath>
    </parent>

    <artifactId>coverage-report</artifactId>
    <packaging>pom</packaging>
     <name>Coverage Aggregate Report</name>
     <!-- This module exists ONLY to aggregate coverage across bounded contexts -->
     <dependencies>
        <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>apprentice-api</artifactId>
            <version>${revision}</version>
        </dependency>
         <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>apprentice-impl</artifactId>
            <version>${revision}</version>
        </dependency>
         <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>audit-log-api</artifactId>
            <version>${revision}</version>
        </dependency>
         <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>audit-log-impl</artifactId>
            <version>${revision}</version>
        </dependency>
         <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>consent-impl</artifactId>
            <version>${revision}</version>
        </dependency>
		<dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>education-instructor-api</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>education-instructor-impl</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>education-api</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>education-impl</artifactId>
            <version>${revision}</version>
        </dependency>
         <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>education-line-api</artifactId>
            <version>${revision}</version>
        </dependency>
         <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>education-line-impl</artifactId>
            <version>${revision}</version>
        </dependency>
		<dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>education-line-saga-api</artifactId>
            <version>${revision}</version>
        </dependency>
         <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>education-line-saga-impl</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>education-saga-api</artifactId>
            <version>${revision}</version>
        </dependency>
         <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>education-saga-impl</artifactId>
            <version>${revision}</version>
        </dependency>
         <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>institution-api</artifactId>
            <version>${revision}</version>
        </dependency>
         <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>institution-impl</artifactId>
            <version>${revision}</version>
        </dependency>
         <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>instructor-api</artifactId>
            <version>${revision}</version>
        </dependency>
         <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>instructor-impl</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>organisation-api</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>organisation-impl</artifactId>
            <version>${revision}</version>
        </dependency>
         <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>person-api</artifactId>
            <version>${revision}</version>
        </dependency>
         <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>person-impl</artifactId>
            <version>${revision}</version>
        </dependency>
         <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>sop-api</artifactId>
            <version>${revision}</version>
        </dependency>
         <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>sop-impl</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>consent-saga-api</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>local.sop.datawarehouse</groupId>
            <artifactId>consent-saga-impl</artifactId>
            <version>${revision}</version>
        </dependency>
		<dependency>
			<groupId>local.sop.datawarehouse</groupId>
			<artifactId>registration-saga-api</artifactId>
			<version>${revision}</version>
		</dependency>
		<dependency>
			<groupId>local.sop.datawarehouse</groupId>
			<artifactId>registration-saga-impl</artifactId>
			<version>${revision}</version>
		</dependency>

         <dependency>
             <groupId>local.sop.datawarehouse</groupId>
             <artifactId>anonymize-api</artifactId>
             <version>${revision}</version>
         </dependency>
         <dependency>
             <groupId>local.sop.datawarehouse</groupId>
             <artifactId>anonymize-impl</artifactId>
             <version>${revision}</version>
         </dependency>
         <dependency>
             <groupId>local.sop.datawarehouse</groupId>
             <artifactId>anonymize-saga-api</artifactId>
             <version>${revision}</version>
         </dependency>
         <dependency>
             <groupId>local.sop.datawarehouse</groupId>
             <artifactId>anonymize-saga-impl</artifactId>
             <version>${revision}</version>
         </dependency>
         <dependency>
                <groupId>local.sop.datawarehouse</groupId>
                <artifactId>sop-education-api</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>local.sop.datawarehouse</groupId>
                <artifactId>sop-education-impl</artifactId>
                <version>${revision}</version>
            </dependency>
     </dependencies>
    <build>
    <plugins>
    <!-- Disable enforcer ONLY for this module -->
    <plugin>
      <groupId>org.apache.maven.plugins</groupId>
      <artifactId>maven-enforcer-plugin</artifactId>
      <version>${enforcer.version}</version>
      <configuration>
        <skip>true</skip>
      </configuration>
    </plugin>
      <!-- Only aggregate here – no agents, de leger børnene allerede med -->
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>0.8.12</version>
        <executions>
          <execution>
            <id>report-aggregate</id>
            <phase>verify</phase>
            <goals>
              <goal>report-aggregate</goal>
            </goals>
            <configuration>
              <!-- Brug de per-modul merged.exec, som du allerede genererer -->
              <dataFileIncludes>
                <dataFileInclude>**/target/jacoco-output/merged.exec</dataFileInclude>
              </dataFileIncludes>

              <outputDirectory>
                ${project.reporting.outputDirectory}/jacoco-aggregate
              </outputDirectory>

              <!-- Vi vil ikke se coverage-report-modulet selv -->
              <includeCurrentProject>false</includeCurrentProject>
            </configuration>
          </execution>
        </executions>
      </plugin>
            <!-- activate flatten-plugin, config/vers. from parent -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
            </plugin>
    </plugins>
  </build>
</project>